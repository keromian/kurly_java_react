git init

git config user.name 'keromian'

git config user.email 'keromian@gmail.com'

git remote add origin https://github.com/keromian/kurly_java_react.git

git remote -v

git add .

git commit -m '마켓컬리 자바기반 리액트 프로젝트'

(git push origin -b)

git push origin master